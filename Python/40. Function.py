# Exercise
# Write the print_seq function for Sam, which itself displays the resulting repetitions. 
# It takes two arguments, a string and a number, 
# and prints a repeating string to the screen. To generate a string, use “duplication” of strings.

def print_seq(string, number):
    print(string * number)
#or
def func(a, b):
    print(a*b)
    
