# Exercise
# The type function allows you to determine the type of the argument being passed.
# The type name is returned as a string. For example, calling type (10) will return the string <class 'int'> 
# (int, this abbreviation for integer is an integer).

# Display the value type of the motto variable.

motto = 'Family, Duty, Honor';

# BEGIN
print(type(motto))
# END
