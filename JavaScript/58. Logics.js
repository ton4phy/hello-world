// Exercise
// Write an isPensioner function that takes age as the only argument and checks if this age is retirement. Pension is considered the age of 60 years and more.

// Definitions
// The logical type (bool) is a data type with two possible values: true (true) and false (false).

// BEGIN
const isPensioner = age => age >= 60
// END

module.exports = isPensioner;
