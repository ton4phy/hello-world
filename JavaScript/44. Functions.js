// Exercise

// The typeof operator allows you to determine the type of the transmitted operand.
// The type name is returned as a string. 
// For example, calling typeof 'go go go' returns the string 'string' (number is an integer).

// Display the value type of the motto constant.

const motto = 'Family, Duty, Honor';

// BEGIN
console.log(typeof motto);
// END
